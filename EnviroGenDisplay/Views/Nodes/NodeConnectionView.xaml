<UserControl x:Class="EnviroGenDisplay.Views.Nodes.NodeConnectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EnviroGenDisplay.Views"
             xmlns:viewModels="clr-namespace:EnviroGenDisplay.ViewModels"
             mc:Ignorable="d">
    <Line d:DataContext="{d:DesignInstance viewModels:NodeConnectionViewModel}" Stroke="Black"
          X1="{Binding Path=One.X}"
          Y1="{Binding Path=One.Y}"
          X2="{Binding Path=Two.X}"
          Y2="{Binding Path=Two.Y}"></Line>
</UserControl>
