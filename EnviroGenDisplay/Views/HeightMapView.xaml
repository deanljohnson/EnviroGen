<UserControl x:Class="EnviroGenDisplay.Views.HeightMapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewModels="clr-namespace:EnviroGenDisplay.ViewModels"
             mc:Ignorable="d" 
             Height="Auto" Width="Auto">
    <Grid>
        <Border BorderThickness="1" BorderBrush="Black" VerticalAlignment="Top" Margin="10,10,10,10">
            <Image d:DataContext="{d:DesignInstance viewModels:EnvironmentViewModel}" HorizontalAlignment="Left" VerticalAlignment="Top" Width="1000" Height="780" Source="{Binding Path=HeightMapBitmap}"/>
        </Border>
    </Grid>
</UserControl>
