<UserControl x:Class="EnviroGenDisplay.Views.StatusTrackerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EnviroGenDisplay.Views"
             xmlns:viewModels="clr-namespace:EnviroGenDisplay.ViewModels"
             mc:Ignorable="d">
    <Grid d:DataContext="{d:DesignInstance viewModels:StatusTrackerViewModel, IsDesignTimeCreatable=True}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.Resources>
            <DataTemplate DataType="{x:Type viewModels:StatusTrackerViewModel}">
                <local:StatusTrackerView/>
            </DataTemplate>
        </Grid.Resources>
        <Label Content="{Binding Path=CurrentMessage, FallbackValue=None}"/>
    </Grid>
</UserControl>
